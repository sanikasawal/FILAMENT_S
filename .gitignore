# ── Secrets & Environment ──
.env
*.env
backend/.env
.env.*
!backend/.env.example

# ── Credentials & keys ──
*.pem
*.key
*.p12
credentials.json
token.json
client_secret*.json
service-account*.json
gcloud-*.json

# ── Documents (hackathon planning docs — not for repo) ──
*.docx
*.html
!docs/index.html
!docs/privacy.html
HANDOFF.md

# ── Reference repos (cloned for patterns, not our code) ──
adk-python/
gemini-live-api-examples/
live-api-web-console/
cli/
Untitled/

# ── Scratch / captured terminal output ──
comaand outpu.md
*-output.md
*.out

# ── Local-only docs (not for the repo) ──
CLAUDE.md
Hackathon Rules.md
PRIVACY.md

# ── Python build artifacts ──
__pycache__/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/
*.py[cod]
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/
.eggs/
*.egg

# ── Virtual environment ──
.venv/
venv/
env/

# ── Chrome Extension build artifacts ──
extension/dist/
extension/node_modules/
*.crx
*.zip

# ── Node / JS ──
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
bun.lockb
package-lock.json

# ── OS ──
.DS_Store
Thumbs.db
desktop.ini

# ── Editor ──
.vscode/
.idea/
*.swp
*.swo
*~
.cursor/

# ── Claude Code ──
.claude/

# ── Axon codebase index ──
.axon/

# ── Logs ──
*.log
